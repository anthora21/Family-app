{
  "name": "family-app",
  "version": "1.0.0",
  "description": "Family Organizer - Baby tracking & task management PWA",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
